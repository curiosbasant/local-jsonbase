import LocalJSONBase from ".."

const jsonDB = new LocalJSONBase("db")
console.log("crated")
// const guilds = jsonDB.collection("guilds")

// const doc = guilds.add({ heloo: 76, city: "srig" })

// setTimeout(async () => {
//   ;(await doc).delete()
// }, 7000)
